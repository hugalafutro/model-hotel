# Copy to .env. Grafana refuses to start without an admin password.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=
GRAFANA_PORT=3000
PROMETHEUS_RETENTION=30d
# The uid/gid that owns tokens/ (`id -u`, `id -g`), so Prometheus can read the 0600 files.
OBS_UID=1000
OBS_GID=1000
